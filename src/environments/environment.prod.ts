const realm = "comuneros"

export const environment = {
    production: true,
    apiUrl: "https://api-comuneros.montesmarcon.com/comuneros/api/v1",
    authClientId: "comuneros-app",
    authIssuer: "https://auth.montesmarcon.com/realms/" + realm,
    webEndpoint: "https://comuneros.montesmarcon.com",

  };


