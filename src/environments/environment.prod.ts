const realm = "comuneros"

export const environment = {
    production: true,
    apiUrl: "https://api-comuneros.miguelmontero.dev/comuneros/api/v1",
    authClientId: "comuneros-app",
    authIssuer: "https://auth-comuneros.miguelmontero.dev/realms/" + realm,
    webEndpoint: "https://comuneros.miguelmontero.dev",

  };


