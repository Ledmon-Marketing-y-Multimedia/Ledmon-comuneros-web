const realm = "comuneros"

export const environment = {
    production: false,
    // apiUrl: "/api/"
    apiUrl: "/api",
    realm: realm,
    authIssuer: "http://localhost:9090/realms/comuneros",
    authClientId: "comuneros-app",
    webEndpoint: "http://localhost:4200"
  };
